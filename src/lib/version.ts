/**
 * we_share — version source of truth
 *
 * Semantic Versioning: vMAJOR.MINOR.PATCH
 *   PATCH  — bug fixes, typo corrections, minor UI tweaks
 *   MINOR  — new features, no breaking changes
 *   MAJOR  — significant rebuild or new direction
 *
 * Pre-launch builds tracked as v0.x.x
 * Change APP_VERSION here on every update, then document it in CHANGELOG.md
 */

export const APP_VERSION = 'v0.1.4'
